# H2 connection
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

# H2 console
  h2:
    console:
      enabled: true
      path: /h2-console

# Show SQL on console
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

