spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/uri2602
    username: postgres
    password: ${dbpassword}

  jpa:
#    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true

    hibernate:
      ddl-auto: none
